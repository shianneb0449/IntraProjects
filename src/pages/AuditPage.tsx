// Audit log page — v1.0.0
export function AuditPage() {
  return (
    <div className="p-6">
      <div className="mb-6">
        <h1 className="text-xl font-semibold font-display">Audit Log</h1>
        <p className="text-sm text-muted-foreground mt-1">
          Full change history across all entities
        </p>
      </div>
      <p className="text-muted-foreground text-sm">
        Audit log — connect to API in v1.0.0
      </p>
    </div>
  );
}
